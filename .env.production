# Cloudflare Pages: leave VITE_PUBLIC_API_ORIGIN unset — public/_redirects proxies /api to Railway.
# Only set this when the UI and API are on different origins without a reverse proxy.
# VITE_PUBLIC_API_ORIGIN=https://modulon-landing-production.up.railway.app
